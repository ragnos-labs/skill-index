# System skills

System skills make the library itself discoverable and safe to use. They do not
perform the domain work described by other skills.

The initial system skill is `skill-index`. It searches compact generated
metadata, verifies the selected bundle, and returns the path to one skill. It
does not execute scripts or grant authority.
